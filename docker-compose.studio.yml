services:
  dolt:
    image: dolthub/dolt-sql-server:latest
    volumes:
      - /Users/bedwards/.doltdb:/var/lib/dolt
    ports:
      - "127.0.0.1:3307:3306"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
